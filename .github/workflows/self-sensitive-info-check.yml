name: Sensitive info check

# Thin caller stub — all logic lives in the sensitive-info-check.yml reusable
# workflow in this same repo (dcwalker/workflows). Kept as a separate file from
# it (rather than calling itself in-place) for the same reason self-review.yml
# is split out: a workflow_call definition and its own self-invocation can't
# share one file.
#
# SENSITIVE_DENYLIST repo secret: one POSIX ERE per line, case-insensitive.
# See sensitive-info-check.yml for full denylist format documentation.

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, edited]
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'  # Mondays 06:00 UTC
  workflow_dispatch: {}

jobs:
  sensitive-info:
    name: Sensitive info check
    uses: dcwalker/workflows/.github/workflows/sensitive-info-check.yml@main
    permissions:
      contents: read
      issues: write
    with:
      event_name: ${{ github.event_name }}
      actor: ${{ github.actor }}
      repository: ${{ github.repository }}
      ref_name: ${{ github.ref_name }}
      run_url: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      pr_base_sha: ${{ github.event.pull_request.base.sha }}
      pr_head_sha: ${{ github.event.pull_request.head.sha }}
      pr_title: ${{ github.event.pull_request.title }}
      pr_body: ${{ github.event.pull_request.body }}
      before_sha: ${{ github.event.before }}
      after_sha: ${{ github.event.after }}
    secrets:
      SENSITIVE_DENYLIST: ${{ secrets.SENSITIVE_DENYLIST }}
      GH_TOKEN: ${{ github.token }}
